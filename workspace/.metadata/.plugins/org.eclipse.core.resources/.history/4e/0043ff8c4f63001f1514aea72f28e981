<%@page import="com.google.gson.Gson"%>
<%@page import="com.itextpdf.text.log.SysoCounter"%>
<%@page import="java.io.BufferedReader"%>
<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>

<%-- comment --%>

<%
	
	request.setCharacterEncoding("UTF-8");

	StringBuffer strbuf = new StringBuffer();
	String line = null;
	
	try{
		BufferedReader reader = request.getReader();
		while((line = reader.readLine())!=null){
			strbuf.append(line);
		}
		
		
	}catch(Exception e){
		e.printStackTrace();
	}

	/* alternate way for above
	while(true){
		line = reader.readLine();
		if(line==null) break;
		strbuf.append(line);
	}*/

	String reqData = strbuf.toString();
	// System.out.print("reqdata: "+reqData);
	
	
	
	// Deserialize
	Gson gson = new Gson();
	
	// gson.fromJSON(str, typeclass)
	
	// we need a java class which is typeclass
	// class that has property of id, name, email, need to be created as VO class. java class must be in src under Java Resources
	
	SeriaVO svo = gson.fromJson
	
%>

